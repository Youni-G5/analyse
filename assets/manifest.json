{
  "name": "{{ shop.name }}",
  "short_name": "{% if settings.site_name_short != blank %}{{ settings.site_name_short }}{% else %}{{ shop.name | truncate: 12, '' }}{% endif %}",
  "description": "{{ shop.description | escape }}",
  "start_url": "/",
  "display": "standalone",
  "background_color": "{{ settings.background_color }}",
  "theme_color": "{% if settings.enable_theme_color and settings.theme_color != blank %}{{ settings.theme_color }}{% else %}{{ settings.background_color }}{% endif %}",
  "icons": [
    {% if settings.favicon != blank %}
    {
      "src": "{{ settings.favicon | image_url: width: 192, height: 192 }}",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "{{ settings.favicon | image_url: width: 512, height: 512 }}",
      "sizes": "512x512",
      "type": "image/png"
    }
    {% endif %}
  ]
}